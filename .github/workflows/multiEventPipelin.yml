name: "multiebvent pipeline"
on:
  push:
    branches:
      - master
      - test

jobs:
  on-push:
    runs-on: ubuntu-latest
    steps:
      - name: "running on master/test"
        run: echo "running on master/test push"
